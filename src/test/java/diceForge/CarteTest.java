package diceForge;

//import bot.TestBot;
import org.junit.Test;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import static org.junit.Assert.*;

public class CarteTest {
    /*
    private Joueur j1 = new TestBot(1, new Afficheur(false), null);
    private Joueur j2 = new TestBot(2, new Afficheur(false), null);

    @Test
    public void effetDirectSanglier(){
        List<Joueur> listeJ = new ArrayList<>(Arrays.asList(j1, j2));
        Carte sanglier = new CarteSanglier(listeJ);
        TestBot j1T = (TestBot) j1;
        TestBot j2T = (TestBot) j2;
        j1T.setId(2);
        j2T.setNumDe(0);
        j2T.setNumFace(0);
        j1.acheterExploit(sanglier);
        assertEquals(new FaceSanglier(j1).toString(), j2.getDes()[0].getFace(0).toString());
    }
    */

}
